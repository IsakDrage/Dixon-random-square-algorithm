This project implements Dixon's random squares algorithm for integer factorization in Sagemath. Which returns a random factor of the integer input.

The algorithm searches for congruent squares modulo n by collecting smooth numbers
and solving a linear system over GF(2).

Tools used:
- Python
- SageMath
- Linear algebra over finite fields
